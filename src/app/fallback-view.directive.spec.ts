import { FallbackViewDirective } from './fallback-view.directive';

describe('FallbackViewDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackViewDirective();
    expect(directive).toBeTruthy();
  });
});
