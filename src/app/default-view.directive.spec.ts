import { DefaultViewDirective } from './default-view.directive';

describe('DefaultViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultViewDirective();
    expect(directive).toBeTruthy();
  });
});
